#include <windows.h>
#include <stdlib.h>
 
int main()  {

	return 0;
}
/*
void function()
{
	while (1) 
	{
		
	}
}


int main () {
	STARTUPINFO info = { sizeof(info) };
	PROCESS_INFORMATION processInfo;
	
	if(CreateProcess("C:\Programming\C\Windows\CALC\Debug\CALC.exe",0,NULL,NULL,TRUE,0,NULL,NULL,&info,&processInfo);)
	{
	CreateProcess("C:\Programming\C\Windows\CALC\Debug\CALC.exe",0,NULL,NULL,TRUE,0,NULL,NULL,&info,&processInfo);
	
	
	WaitForSingleObjcet(processInfo.hProcess,INFINITE);
	CloseHandle(processInfo.hProcess);
	CloseHandle(processInfo.hThread);
	}


	DWORD myThreadID;
	HANDLE myHandle = CreateThread(0,0,(PTHREAD_START_ROUTINE)function,0,0,&myThreadID);
	
	//CloseHandle(processInfo.hProcess);
	//CloseHandle(processInfo.hThread);
	
	return 0;
}
*/